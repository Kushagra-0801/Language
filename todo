1. File read -> String
2. String -> Character Iterator -> Peekable Iter
3. Functions (token)
    | Integer
    | Float
    | Idents (keyword)
    | Symbols (+, -, ., :, ;, :: etc)


lang file.l


src
    | main.cpp
    | lexer.cpp
        | LexState (Character Iterator, dfa state, tokens list)
            | Proceed state (loop)
            
